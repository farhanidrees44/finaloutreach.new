/**
 * Route-level loading UI. Kept intentionally empty so marketing pages
 * (especially the homepage) do not flash a full-screen "Loading" spinner
 * before streaming content. Prefer skeleton patterns inside individual
 * components when needed.
 */
export default function Loading() {
  return null
}
